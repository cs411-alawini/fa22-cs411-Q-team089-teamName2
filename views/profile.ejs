<!DOCTYPE html>
<html lang="en">
 <head>
   <title>TeamName2 Project</title>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
 </head>

  <body>
    <div class="container mt-4">
      <h1 class="text-center"><%=names%></h1>
    </div>
    <div class="container mt-4">
      <a href="" class="btn btn-primary mt-4">Stats</a>
      <a href="/checklist/landing?userId=<%= userId%>" class="btn btn-primary mt-4">My Checklists</a>
      <a href="/groups/landing?userId=<%= userId%>" class="btn btn-primary mt-4">My Groups</a>
      <div id="accordian">
        <% data.forEach(item => {%>
          <div class="card m-2">
            <div class="card-body">
              <div class="row">
                <h4 class="col-sm mx-4"><%= item.messages%></h4>
                <a href="/tasks/landing?userId=<%= userId%>&checklistId=<%= item.checkListId%>" class="btn btn-primary col-sm mx-4">Go to Task</a>
              </div>
            </div>
          </div>
          <% }) %>
      </div>
    </div>
  </body>
</html>